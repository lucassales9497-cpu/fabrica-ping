name: Manter a Fabrica acordada
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping na API
        run: curl -fsS --max-time 60 --retry 2 https://fabrica-contratos-api.onrender.com/health